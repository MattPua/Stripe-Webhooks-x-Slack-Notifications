# Stripe
# Required: webhook signing secret from Stripe Dashboard → Developers → Webhooks (endpoint-specific)
# Example: whsec_abc123
STRIPE_WEBHOOK_SECRET=

# Required: Incoming webhook (posts to a fixed channel configured in Slack)
SLACK_WEBHOOK_URL=


# Optional event filtering: comma-separated glob patterns
# Examples:
STRIPE_EVENT_ALLOWLIST=invoice.*,checkout.session.completed,payment_intent.succeeded
STRIPE_EVENT_DENYLIST=*.deleted,charge.dispute.*
